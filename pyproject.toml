[build-system]
requires = [
    "setuptools>=42",
    "versioneer",
    "packaging",
    "amulet-compiler-version@git+https://github.com/Amulet-Team/Amulet-Compiler-Version.git@3.0"
]
build-backend = "build_requires"
backend-path = [""]

[project]
name = "amulet-editor"
authors = [
    {name = "James Clare"},
    {name = "Ben Gothard"},
]
description = "A new Minecraft world editor and converter that supports all versions since Java 1.12 and Bedrock 1.7."
dynamic = ["version", "readme", "dependencies"]
requires-python = ">=3.11"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.optional-dependencies]
dev = [
    "setuptools>=42",
    "versioneer",
    "packaging",
    "wheel",
    "pybind11_stubgen>=2.5.4",
    "black>=22.3",
    "isort",
    "autoflake",
    "mypy",
    "types-pyinstaller",
    "amulet-test-utils~=1.1",
]

[project.urls]
Homepage = "https://www.amuletmc.com"
Repository = "https://github.com/Amulet-Team/Amulet-Editor"
Issues = "https://github.com/Amulet-Team/Amulet-Editor/issues"

[tool.setuptools]
include-package-data = false

[tool.setuptools.package-data]
"*" = [
    "*Config.cmake",
    "**/*.hpp",
    "**/*.dll",
    "**/*.so",
    "**/*.dylib",
    "**/*.lib",
    "**/*.jpg",
    "**/*.png",
    "**/*.svg",
    "**/*.ico",
    "**/*.lang",
    "**/*.json",
    "**/*.qss",
]

[tool.setuptools.dynamic]
readme = {file = ["README.md"], content-type = "text/markdown"}

[project.entry-points.pyinstaller40]
hook-dirs = "amulet_editor.__pyinstaller:get_hook_dirs"

[project.entry-points.console_scripts]
amulet_editor = "amulet_editor.__main__:main"

[project.entry-points.gui_scripts]
amulet_map_editor_no_console = "amulet_editor.__main__:main"

[tool.versioneer]
VCS = "git"
style = "pep440"
versionfile_source = "src/amulet_editor/_version.py"
versionfile_build = "amulet_editor/_version.py"
tag_prefix = ""
parentdir_prefix = "amulet-editor-"
